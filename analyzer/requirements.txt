# ChordScope Analyzer - v1 (madmom focused)
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12

# Audio + Analysis
librosa==0.10.2
soundfile==0.12.1
numba==0.60.0
numpy==1.26.4

# madmom - installed via git for best compatibility on Python 3.10+
# We install it separately in the Dockerfile

# Utilities
pydantic==2.9.2
python-dotenv==1.0.1
tqdm==4.66.5
